services:
  # ─── Web Service (Customer Portal Only) ───────────────────────────────────────
  - type: web
    name: bakery-customer-portal
    runtime: python
    plan: free
    region: singapore
    buildCommand: pip install -r requirements.txt
    preDeployCommand: python scripts/bootstrap_database.py
    startCommand: gunicorn --config gunicorn.conf.py wsgi:app
    healthCheckPath: /healthz

    envVars:
      - key: FLASK_ENV
        value: production
      - key: PORTAL_ROLE
        value: customer
      - key: SECRET_KEY
        generateValue: true
      - key: JWT_SECRET_KEY
        generateValue: true

      # TiDB Cloud - External Database (required in production)
      - key: DATABASE_URL
        sync: false
      - key: DB_SSL_CA
        value: /etc/ssl/certs/ca-certificates.crt
      - key: DB_SSL_VERIFY_CERT
        value: "true"
      - key: DB_SSL_VERIFY_IDENTITY
        value: "true"

      # Redis - Render-managed and shared by Socket.IO, Celery, cache, and rate limiting
      - key: REDIS_URL
        fromService:
          name: bakery-redis
          type: redis
          property: connectionString

      # Mail
      - key: MAIL_SERVER
        sync: false
      - key: MAIL_PORT
        sync: false
      - key: MAIL_USERNAME
        sync: false
      - key: MAIL_PASSWORD
        sync: false
      - key: MAIL_DEFAULT_SENDER
        sync: false

      # Cloudinary
      - key: CLOUDINARY_CLOUD_NAME
        sync: false
      - key: CLOUDINARY_API_KEY
        sync: false
      - key: CLOUDINARY_API_SECRET
        sync: false

      # Twilio
      - key: TWILIO_ACCOUNT_SID
        sync: false
      - key: TWILIO_AUTH_TOKEN
        sync: false
      - key: TWILIO_FROM_NUMBER
        sync: false
      - key: TWILIO_WHATSAPP_FROM
        sync: false

      # Firebase
      - key: FIREBASE_PROJECT_ID
        sync: false
      - key: FIREBASE_CLIENT_EMAIL
        sync: false
      - key: FIREBASE_PRIVATE_KEY
        sync: false

      # Google Maps
      - key: GOOGLE_MAPS_API_KEY
        sync: false

      # Sentry
      - key: SENTRY_DSN
        sync: false

      # Production settings
      - key: USE_PROXY_FIX
        value: "true"
      - key: CUSTOMER_PORTAL_URL
        sync: false
      - key: SHOW_DEMO_ACCOUNTS
        value: "false"
      - key: OFFLINE_SYNC_ENABLED
        value: "false"
      - key: SOCKETIO_ASYNC_MODE
        value: gevent
      - key: RATELIMIT_STORAGE_URI
        fromService:
          name: bakery-redis
          type: redis
          property: connectionString
      - key: SOCKETIO_MESSAGE_QUEUE
        fromService:
          name: bakery-redis
          type: redis
          property: connectionString
      - key: CELERY_BROKER_URL
        fromService:
          name: bakery-redis
          type: redis
          property: connectionString
      - key: CELERY_RESULT_BACKEND
        fromService:
          name: bakery-redis
          type: redis
          property: connectionString

  # ─── Celery Worker ─────────────────────────────────────────────
  - type: worker
    name: bakery-celery-worker
    runtime: python
    plan: free
    region: singapore
    buildCommand: pip install -r requirements.txt
    startCommand: celery -A celery_app.celery worker --loglevel=info --concurrency=2

    envVars:
      - key: FLASK_ENV
        value: production
      - key: PORTAL_ROLE
        value: customer
      - key: SECRET_KEY
        fromService:
          name: bakery-customer-portal
          type: web
          envVarKey: SECRET_KEY
      - key: JWT_SECRET_KEY
        fromService:
          name: bakery-customer-portal
          type: web
          envVarKey: JWT_SECRET_KEY
      - key: DATABASE_URL
        fromService:
          name: bakery-customer-portal
          type: web
          envVarKey: DATABASE_URL
      - key: DB_SSL_CA
        fromService:
          name: bakery-customer-portal
          type: web
          envVarKey: DB_SSL_CA
      - key: DB_SSL_VERIFY_CERT
        fromService:
          name: bakery-customer-portal
          type: web
          envVarKey: DB_SSL_VERIFY_CERT
      - key: DB_SSL_VERIFY_IDENTITY
        fromService:
          name: bakery-customer-portal
          type: web
          envVarKey: DB_SSL_VERIFY_IDENTITY
      - key: REDIS_URL
        fromService:
          name: bakery-redis
          type: redis
          property: connectionString
      - key: RATELIMIT_STORAGE_URI
        fromService:
          name: bakery-redis
          type: redis
          property: connectionString
      - key: SOCKETIO_MESSAGE_QUEUE
        fromService:
          name: bakery-redis
          type: redis
          property: connectionString
      - key: CELERY_BROKER_URL
        fromService:
          name: bakery-redis
          type: redis
          property: connectionString
      - key: CELERY_RESULT_BACKEND
        fromService:
          name: bakery-redis
          type: redis
          property: connectionString
      - key: CLOUDINARY_CLOUD_NAME
        fromService:
          name: bakery-customer-portal
          type: web
          envVarKey: CLOUDINARY_CLOUD_NAME
      - key: CLOUDINARY_API_KEY
        fromService:
          name: bakery-customer-portal
          type: web
          envVarKey: CLOUDINARY_API_KEY
      - key: CLOUDINARY_API_SECRET
        fromService:
          name: bakery-customer-portal
          type: web
          envVarKey: CLOUDINARY_API_SECRET
      - key: MAIL_SERVER
        fromService:
          name: bakery-customer-portal
          type: web
          envVarKey: MAIL_SERVER
      - key: MAIL_PORT
        fromService:
          name: bakery-customer-portal
          type: web
          envVarKey: MAIL_PORT
      - key: MAIL_USERNAME
        fromService:
          name: bakery-customer-portal
          type: web
          envVarKey: MAIL_USERNAME
      - key: MAIL_PASSWORD
        fromService:
          name: bakery-customer-portal
          type: web
          envVarKey: MAIL_PASSWORD
      - key: MAIL_DEFAULT_SENDER
        fromService:
          name: bakery-customer-portal
          type: web
          envVarKey: MAIL_DEFAULT_SENDER
      - key: TWILIO_ACCOUNT_SID
        fromService:
          name: bakery-customer-portal
          type: web
          envVarKey: TWILIO_ACCOUNT_SID
      - key: TWILIO_AUTH_TOKEN
        fromService:
          name: bakery-customer-portal
          type: web
          envVarKey: TWILIO_AUTH_TOKEN
      - key: TWILIO_FROM_NUMBER
        fromService:
          name: bakery-customer-portal
          type: web
          envVarKey: TWILIO_FROM_NUMBER
      - key: TWILIO_WHATSAPP_FROM
        fromService:
          name: bakery-customer-portal
          type: web
          envVarKey: TWILIO_WHATSAPP_FROM
      - key: FIREBASE_PROJECT_ID
        fromService:
          name: bakery-customer-portal
          type: web
          envVarKey: FIREBASE_PROJECT_ID
      - key: FIREBASE_CLIENT_EMAIL
        fromService:
          name: bakery-customer-portal
          type: web
          envVarKey: FIREBASE_CLIENT_EMAIL
      - key: FIREBASE_PRIVATE_KEY
        fromService:
          name: bakery-customer-portal
          type: web
          envVarKey: FIREBASE_PRIVATE_KEY
      - key: GOOGLE_MAPS_API_KEY
        fromService:
          name: bakery-customer-portal
          type: web
          envVarKey: GOOGLE_MAPS_API_KEY
      - key: SENTRY_DSN
        fromService:
          name: bakery-customer-portal
          type: web
          envVarKey: SENTRY_DSN
      - key: USE_PROXY_FIX
        value: "true"
      - key: SHOW_DEMO_ACCOUNTS
        value: "false"
      - key: OFFLINE_SYNC_ENABLED
        value: "false"
      - key: SOCKETIO_ASYNC_MODE
        value: gevent

  # ─── Redis (for SocketIO, Celery, Cache, Rate Limiting) ──────────────────────
  - name: bakery-redis
    type: redis
    plan: free
    ipAllowList: []
