SECRET_KEY=delivery-secret-change-me
DATABASE_URL=mysql+pymysql://root:password@localhost/bakery_central
# Use the same Render DB external URL here if delivery runs locally against production data.
# DATABASE_URL=postgresql://USER:PASSWORD@EXTERNAL_HOST:5432/DATABASE
PORT=5002
